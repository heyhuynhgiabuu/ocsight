# DISABLED: NPM publishing is handled by semantic-release in release.yml
# This workflow is redundant and conflicts with automated releases

name: Publish to NPM (DISABLED)

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    if: false # Always skip this job

    steps:
      - name: Skipped
        run: echo "NPM publishing handled by semantic-release"
