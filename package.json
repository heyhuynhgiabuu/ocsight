{
  "name": "ocsight",
  "version": "0.1.0",
  "description": "OpenCode ecosystem observability platform - see everything happening in your OpenCode development",
  "main": "dist/index.js",
  "bin": {
    "ocsight": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "clean": "rm -rf dist",
    "start": "node dist/index.js",
    "mcp": "bun run src/mcp/server.ts",
    "test": "NODE_ENV=test bun test",
    "test:watch": "NODE_ENV=test bun test --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "csv-writer": "^1.6.0",
    "ora": "^8.0.0",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.0.0"
}
