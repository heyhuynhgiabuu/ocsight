{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "lib/**"]
    },
    "bundle-cli": {
      "dependsOn": ["build"],
      "outputs": ["dist-bundle/**", "lib/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"]
    },
    "package": {
      "dependsOn": ["bundle"],
      "outputs": ["dist-bundle/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
